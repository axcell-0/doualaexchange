"use client";

export default function ExchangerPrimaryAction() {
  return (
    <button className="w-full bg-[#0ff05a] text-background-dark font-bold py-4 rounded-xl shadow-lg flex items-center justify-center gap-3 hover:scale-[1.02] active:scale-[0.98] transition-all">
      <span className="material-symbols-outlined font-bold">radar</span>
      FIND NEW OFFERS NEARBY
    </button>
  );
}
